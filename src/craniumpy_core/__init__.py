"""Core geometry/algorithm library for CraniumPy.

Pure computation: mesh objects in, mesh objects or measurements out.
No GUI, no rendering, no file dialogs, no disk round-tripping between
pipeline steps. Both the web API (api/) and the desktop app (desktop/)
call into this package; neither owns any algorithm logic.
"""

__version__ = "0.1.0"
